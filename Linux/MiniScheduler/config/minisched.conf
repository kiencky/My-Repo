# minisched.conf - MiniScheduler configuration file

# Number of worker processes.
num_workers=4

# Path for the Unix domain socket used for communication between daemon and client.
socket_path=/tmp/minisched.sock